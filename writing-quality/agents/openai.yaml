interface:
  display_name: "Writing Quality"
  short_description: "Plain, natural prose on every human-facing reply"
  default_prompt: "Use $writing-quality to draft or revise this response in clear, natural language."
policy:
  allow_implicit_invocation: true
